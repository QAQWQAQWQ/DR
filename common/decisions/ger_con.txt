ger_con_decisions = {
  ger_con_get_ger_sporrt = {
		visible = {
        NOT = {
          OR = {
            original_tag = AOX
            original_tag = WWS
          }
        }
        check_variable = { gercon_nat = ROOT}
		}		
    available = {
    }
    cost = 50
    fire_only_once = no
    days_remove = 50
    complete_effect = {
      add_to_variable = { var = ROOT.ger_support value = 10 }  
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }
  ger_con_suppress_ger = {
		visible = {
        NOT = {
          OR = {
            original_tag = AOX
            original_tag = WWS
          }
        }
        check_variable = { gercon_nat = ROOT}
		}		
    available = {
    }
    cost = 50
    fire_only_once = no
    days_remove = 30
    complete_effect = {
      add_to_variable = { var = ROOT.ger_support value = -10 }  
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }
  ger_con_suppress_aus = {
		visible = {
        NOT = {
          OR = {
            original_tag = AOX
            original_tag = WWS
          }
        }
        check_variable = { gercon_nat = ROOT}
		}		
    available = {
        var:gercon_nat = {
           check_variable = { ger_con_cangetsupport = 1 }
        }
    }
    cost = 0
    fire_only_once = no
    days_remove = 50
    complete_effect = {
      add_to_variable = { var = ROOT.aus_support value = -10 }  
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }
  ger_con_suppress_pre = {
		visible = {
      NOT = {
        OR = {
          original_tag = AOX
          original_tag = WWS
        }
      }
      check_variable = { gercon_nat = ROOT}
		}		
    available = {
        var:gercon_nat = {
           check_variable = { ger_con_cangetsupport = 1 }
        }
    }
    cost = 0
    fire_only_once = no
    days_remove = 50
    complete_effect = {
      add_to_variable = { var = ROOT.pre_support value = -10 }  
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }
  ger_con_get_support = {
		visible = {
        OR ={
          original_tag = AOX
          original_tag = WWS
        }
        NOT = {
          OR = {
            check_variable = { gercon_nat = AOX}
            check_variable = { gercon_nat = WWS}
          }
        }
		}		
    available = {
        var:gercon_nat = {
           check_variable = { ger_con_cangetsupport = 1 }
        }
    }
    cost = 25
    fire_only_once = no
    days_remove = 10
    complete_effect = {
      var:gercon_nat = {
          if = {
            limit = { ROOT = { original_tag = AOX} }
            add_to_variable = { var = aus_support value = 5 }   
            else = { 
              add_to_variable = { var = pre_support value = 5 } 
            }
          }
      }
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }
  ger_con_ind_inv = {
		visible = {
        OR ={
          original_tag = AOX
          original_tag = WWS
        }
        NOT = {
          OR = {
            check_variable = { gercon_nat = AOX}
            check_variable = { gercon_nat = WWS}
          }
        }
		}		
    available = {
        var:gercon_nat = {
           check_variable = { ger_con_cangetsupport = 1 }
        }
    }
    cost = 100
    fire_only_once = no
    days_remove = 50
    complete_effect = {
      var:gercon_nat = {
          random_owned_state = {
              add_extra_state_shared_building_slots = 1
							add_building_construction = {
									type = industrial_complex
									level = 1
									instant_build = yes
							}
          }
          if = {
            limit = { ROOT = { original_tag = AOX} }
            add_to_variable = { var = aus_support value = 20 }   
            else = { 
              add_to_variable = { var = pre_support value = 20 } 
            }
          }
      }
      gercon_upd_pbv_var = yes
      gercon_clamp_vars = yes
    }
    remove_effect = {
    }
  }

  great_ger_con = {
		icon = generic_civil_support

		available = {
      always = no
		}

		days_mission_timeout = 50

		activation = {
      OR = {
        original_tag = AGC
        original_tag = LSB
        original_tag = DHD
        original_tag = DON
        original_tag = DOF
        original_tag = DOB
        original_tag = DOR
        original_tag = BXM
        original_tag = DOL
        original_tag = DOH
        original_tag = DOA
        original_tag = DOM
        original_tag = DOS
      }
		}

		visible = {
      OR = {
        original_tag = AGC
        original_tag = LSB
        original_tag = DHD
        original_tag = DON
        original_tag = DOF
        original_tag = DOB
        original_tag = DOR
        original_tag = BXM
        original_tag = DOL
        original_tag = DOH
        original_tag = DOA
        original_tag = DOM
        original_tag = DOS
      }
		}		

		fixed_random_seed = no

		fire_only_once = no

		is_good = no

		timeout_effect = {
      add_to_variable = {
        var = ger_support
        value = 10
      }   
      gercon_clamp_vars = yes
		}
	}
}

